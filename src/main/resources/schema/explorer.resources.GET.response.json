// GET /resources?application=&resource_type=&id=&order_by=name:asc|desc&visibility=private|shared|public&favorite=true|false&folder=id&search=FULL_TEXT_SEARCH&start_idx=X&page_size=Y

{
  folders:[{
    id: string,
    name: string,
    type: "bin"|"default"|id, // customisation du dossier (icone...)
    childNumber: number // à minima, 0 ou 1...
  }],
  pagination: { // TODO à tester
    startIdx: 0,
    maxIdx?: number, // Si elastic search renvoie bien le nombre de hits 
    pageSize: 20 // Sera égal à la valeur paramétrée côté serveur
  },
  resources:[{
    id: string,
    name: string,
    thumbnail: URL, // requis, ou bien déductible d’une convention ?
    application: string,
    createdAt: date,
    authorId: string,
    authorName: string,
    modifierId: string,
    modifierName: string,
    modifiedAt: date,
    folderId?: string, // TODO à confirmer
    visibility?: "private"|"shared"|"public",
    favorite?: boolean,
    views?: number,
    comments?: number
  }]
}