// GET /explorer/context?application=

{
  folders:[{
    id: string,
    name: string,
    type: "bin"|"default"|id, // customisation du dossier (icone...)
    childNumber: number // à minima, 0 ou 1...
  }],
  filters:[{
    id: string, // ex: "visibility"
    defaultValue?: string|boolean,
    values:[{
      value: string|boolean, // ex: "private" ou "shared" ou "public"
      name: string, // Clé du texte à traduire
    }]
  }],
  orders:[{
    id: string, // ex: "name" ou "date_modif", faire une enum
    defaultValue?: "asc"|"desc",
    name: string // Clé du texte à traduire
  }],
  actions: [{
    id: string, //ex: "create" par convention
    available: boolean  // L'utilisateur a le droit workflow ou pas
    //TODO comment relier les actions aux behaviours, qu'on va remplacer.
  }],
  pagination: { // TODO à tester
    startIdx: 0,
    maxIdx?: number, // Si elastic search renvoie bien le nombre de hits 
    pageSize: 20 // Sera égal à la valeur paramétrée côté serveur
  },
  resources:[{
    id: string,
    name: string,
    thumbnail: URL, // requis, ou bien déductible d’une convention ?
    application: string,
    createdAt: date,
    authorId: string,
    authorName: string,
    modifierId: string,
    modifierName: string,
    modifiedAt: date,
    folderId?: string, // TODO à confirmer
    public?: boolean,
    shared?: boolean,
    favorite?: boolean,
    views?: number,
    comments?: number
  }],
  preferences: {
    view: "card"|"list"
    // TODO autres ?
  }
}